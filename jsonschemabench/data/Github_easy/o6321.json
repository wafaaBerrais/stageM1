{
    "definitions": {
        "example": {
            "instances": []
        }
    },
    "modelId": {
        "pattern": "[a-z]([-a-z0-9]*[a-z0-9])",
        "type": "string"
    },
    "projectId": {
        "pattern": "[a-z]([-a-z0-9]*[a-z0-9])",
        "type": "string"
    },
    "required": [
        "modelId",
        "projectId",
        "versionId"
    ],
    "testExamples": [
        {
            "$ref": "#/definitions/example"
        }
    ],
    "versionId": {
        "pattern": "[a-z]([-a-z0-9]*[a-z0-9])",
        "type": "string"
    },
    "properties": {
        "modelId": {
            "$ref": "#/modelId"
        },
        "projectId": {
            "$ref": "#/projectId"
        },
        "versionId": {
            "$ref": "#/versionId"
        }
    }
}